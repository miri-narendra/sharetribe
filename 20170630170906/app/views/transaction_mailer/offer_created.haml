%tr
  %td{:align => "left"}
    %font{body_font}

      = t("emails.new_offer.has_sent_you_an_offer", :sender => sender_name)

= render :partial => "person_mailer/action_button", :locals => { :text => t("emails.new_offer.view_offer"), :url => reply_url}
