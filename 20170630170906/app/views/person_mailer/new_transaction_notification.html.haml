A new transaction created in #{@community.full_name(@owner.locale)}.

%div{:style => "margin: 15px 0; padding: 10px; background-color: #d1c9b4;"}
  %b Owner Name:
  = @owner.name(@community)
  %br/
  %b Owner Email:
  = @owner_email
  %br/
  %b Owner Phone:
  = @owner.phone_number
  %br/
  %b Vehicle requested:
  = @transaction.listing.title
  %br/
  - renter = @transaction.buyer
  %b Renter Name:
  = renter.name(@community)
  %br/
  %b Renter Email:
  = renter.emails.last.address
  %br/
  %b Renter Phone:
  = renter.phone_number
  %br/
  %b date from:
  = l(@transaction.booking.start_on, format: :default)
  %br/
  %b date to:
  = l(@transaction.booking.end_on, format: :default)
  %br/
  %b Pickup location:
  = @transaction.cart.pickup_location
  %br/
  %b Drop off location:
  = @transaction.cart.dropoff_location
  %br/
  %b Renter message:
  = @transaction.conversation.last_message_by(renter).content
  %br/

This is an automatic message sent to administrators of #{@community.full_name(@owner.locale)}.
